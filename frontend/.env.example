# Frontend configuration — copy to `frontend/.env` (never commit the real one)
# Leave empty in dev: the Vite dev server proxies /api to the backend automatically.
# Set only when the API lives on a different origin than the frontend.
VITE_API_URL=
